public class Solution {
  public boolean canChange (String start, String target) {
    //? 1. Iterating target by index starting from 0
    //? 2. Check if current is blank space
      //? (No blank space) iterate while current different than blank space
      //? Check if current is the opposite value -> ex. current = 'R' and we find 'L' -> return false
      //? Try to make move

    return false;
  }
}
