public class Solution {
  public int minimizedMaximum (int n, int[] quantities) {
    /* * 
     * n -> number of specialty retail stores
     * quantities[i] -> number of products 
     * return -> the maximum number minimum possible of 
    */
    return 0;
  }
}
