public class Solution {
  public int[] sameEndSubstringCount (String s, int[][] queries) {
    /*
     * input -> String s, int[][] queries
     * String s -> 0-indexed string
     * int[][] queries -> where queries[i] represents a substring of s starting from the index [li, ri]
     * 
    */
    /*
     * Output -> return array of integers
     * ans -> where ans[i] is the number of same-end substrings of queries[i]
     * 
     */
    return null;
  }
}
