public class Solution {

  // ? Create a variable to store the max index
  //?
  // ? Iterate over the array finding the absolute difference with current number with zero

  public int findClosestNumber (int[] nums) {

    return -1;
  }
}
