public class Solution {
  public long countFairPairs (int[] nums, int lower, int upper) {
    /* 
     * INPUT:
     * nums: array of integers (unsorted)
     * lower: min key value to search 
     * upper: max key value to search
     * OUTPUT: 
     * fairPais: number of fair pairs that satifies the condition
     * SOLUTION
     * Sorting the array
     * Create two pointer-> left = nums[0] && right = nums[nums.length - 1]
     * Initialize fairPairs = 0
     * Iterate through the array while left != right
      * Check if nums[left] >= lower && nums[right] <= upper 
        * 
     */
    return 0;
  }
}
