public class Solution {
  // ? Problem statement: given an array of integers, find the maximum sum of a subarray of a fixed size "k"
  public int maxSumSubarray (int[] nums, int k) {
    int maxSum = 0;
    int windowSum = 0;
    
    for (int i = 0; i < k; i++) {
      
    }
  }
}
