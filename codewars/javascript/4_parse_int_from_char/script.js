// ! Parsing int from char

function getAge (inputString) {
  // ? There does exist a function in javascript for that
  // ? parseInt is made to convert from a data type to Integer
  return parseInt(inputString);
}