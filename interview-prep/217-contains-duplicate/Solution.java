
// ? Initial approach
// ? Using a Set -> Creating a Set
// ? Iterating the String input 
// ? if the set doesn't contain the current char, add it to the set 
// ? If the set contains the character, return true
// ? If the string is traversed successfully, it does not contain any duplicates -> return false

