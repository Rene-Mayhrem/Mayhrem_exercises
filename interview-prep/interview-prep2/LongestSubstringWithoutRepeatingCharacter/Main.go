package longestsubstringwithoutrepeatingcharacter

func longestSubstring(word string) int {
	//? Max length
	maxSubstring := 0
	//? Create a set
	substring := make(map[rune]struct{})
	//? Create an array of char
	runes := []rune(word)
	//? Pointers
	slow := 0
	for fast := 0; fast < len(word); fast++ {

	}
	return maxSubstring
}
